# Bringup config

Launch files under `../launch` select MoveIt controller YAML from
`six_motor_moveit_config`:

- view mode → `controllers.yaml` (mock arm + movable rail)
- hardware mode → `controllers_real.yaml` (real arm + rail hold)
